// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User_DB {
  id        Int       @id @default(autoincrement())
  user_id   String    @unique
  name      String
  email     String
  // 他のカラムも追加することができます
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  // 仮予定との関連を定義
  tentativeSchedules TentativeSchedule_DB[]
}

model TentativeSchedule_DB {
  id               Int            @id @default(autoincrement())
  TentativeScheduleId String
  title            String
  description      String?
  duration         Int           // 所要時間（必要に応じて追加）
  startSecureTime  DateTime      // 確保開始時間（必要に応じて追加）
  endSecureTime    DateTime      // 確保終了時間（必要に応じて追加）
  margin           Int?           // マージンの時間（必要に応じて追加）
  createdAt        DateTime       @default(now())
  // ユーザーとの関連を定義
  user             User_DB           @relation(fields: [userId], references: [id])
  userId           Int
  // 仮日程との関連を定義
  tentativeDates   TentativeDate_DB[]
}

model TentativeDate_DB {
  id                   Int       @id @default(autoincrement())
  tentativeDateId      String
  startTime            DateTime // 開始時間（必要に応じて追加）
  endTime              DateTime // 終了時間（必要に応じて追加）
  // 仮予定との関連を定義
  tentativeSchedule    TentativeSchedule_DB @relation(fields: [tentativeScheduleId], references: [id])
  tentativeScheduleId  Int
}
